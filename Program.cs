namespace ManageAccountApp
{
    class Program
    {
        static void Main(string[] args)
        {
            // Initialize the console UI
            var consoleUI = new UI.ConsoleUI();
            consoleUI.Run();
        }
    }
}